enum BookTypes {
  HAPPY
  SAD
  BATTLE
}

type Content {
  text: String!
}

type Post {
  id:        ID!
  title:     String!
  content:   Content
  bookType:  BookTypes
  published: Boolean
  author:    User
  authorId:  Int
}

type Query {
	getPosts: [Post!]!
}
